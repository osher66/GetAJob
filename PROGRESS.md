# 📋 GetAJob — יומן התקדמות ותמונת מצב (PROGRESS.md)

קובץ זה מיועד לשליפה מהירה ורזה של מצב הפרויקט, תוך חיסכון מרבי בטוקנים בכל סבב עבודה.

---

## 🟢 1. סטטוס מערכת נוכחי (Current Status)
* **שרת Streamlit**: רץ ברקע (`http://localhost:8501`).
* **סביבת עבודה**: Python 3.12 בסביבה וירטואלית (`.venv`), כל התלויות מותקנות.
* **שפת עיצוב מחייבת**: `Claude Design Handoff v1.0` (מצב Light, פלטת אינדיגו `#4F46E5`, טיפוגרפיית Heebo בלבד, ללא אימוג'י).

---

## 🧩 2. רכיבים ומסכים פעילים (Active Modules)
* **דף נחיתה ו-Hero (`app.py`, `ui_styles.py`)**:
  * Navbar מרחף מטושטש, Hero מיושר למרכז.
  * כרטיסיות ערך (אייקוני SVG מימין לכותרת).
  * סקשן "איך זה עובד" מיושר ומאוזן.
  * דוגמת פלט מהודקת עם מד התאמה (64%).
  * שאלות נפוצות (FAQ) במבנה 2x2.
  * באנר CTA כהה תחתון (`#111114`).
* **מסך הרשמה והתחברות (`auth_view.py`)**:
  * שימור מלא ומושלם של ה-Header הקיים של GetAJob.
  * כרטיס טופס ראשי עם מתג הרשמה/התחברות/איפוס סיסמה/אזור אישי.
  * שדות חובה בלבד: "שם ושם משפחה", אימייל, סיסמה עם מד חוזק 3 פסים, אימות סיסמה ותנאי שימוש.
  * פאנל צדדי "למה בכלל חשבון" עם שמירת נתונים ומעקב התקדמות, היסטוריית ניתוחים ומעקב מוכנות.
  * ניהול משתמשים מקומי ומאובטח (`data/users.json`).
* **מנוע וקטלוג נתונים (`career_catalog.py`, `career_domains.py`)**:
  * מאגר תפקידים (UX/UI, Data, Dev, DevOps, QA, Cyber וכו').
  * סילבוס ומסלולי למידה מותאמים לפי מיומנויות חסרות.
* **צ'קליסט אבני דרך אינטראקטיבי (`app.py`)**:
  * מעקב התקדמות מותאם אישית לסגירת פערי מיומנויות עם M3 Linear Progress.
* **מנוע ניתוח ו-LLM (`llm_engine.py`, `schemas.py`, `sanitizer.py`)**:
  * חיבור למודל Gemini עם גיבוי למענה Mock מובנה וניקוי PII מקו"ח.

---

### 📅 09/09/2026 (סבב 7) — שחזור מדויק של העיצוב המקורי מקלוד, הסרת רקעים מיותרים, קיבוע סליידר מימין ו-0.0px קפיצות
* **פעולות שבוצעו**:
  1. **הסרה מוחלטת של רקעים וקופסאות כפולות (Root Cause Fixed)**:
     - בוטלו כללי ה-CSS הגורפים על `stVerticalBlock:has(...)` שגרמו לקופסאות מרובות בתוך קופסאות.
     - הוסר `st.container(border=True)` סביב הטפסים. כעת כל טופס (`stForm`) מהווה כרטיס בודד, נקי, מעוגל (`24px`) ומעוצב ישירות ללא שום שכבות מיותרות.
     - הרקע הכללי חזר להיות הגרדיאנט העדין והחמים של המערכת (`#FBF9F4` עם נגיעות סגולות ואפרסקיות).
  2. **עדכון ה-Header במדויק לדרישת המשתמש**:
     - הלוגו ממוקם במרכז המדויק של הסרגל (`diffFromCenter: 0.0px`), כשהאות `[G]` מוצבת מצד שמאל של הטקסט `GetAJob` (`direction: ltr`).
     - כפתור "חזרה למסך הבית" ממוקם בצד ימין של הסרגל (RTL).
  3. **סליידר גלילה מותאם אישית בצד ימין של הדף בלבד**:
     - הוחל `direction: ltr !important` על מיכל הגלילה הראשי של Streamlit (`section[data-testid="stMain"]`) כדי להבטיח שהסליידר ממוקם בצד ימין של הדף.
     - עוצב סליידר קפסולה דק (8px) בגוון `#D8D2C4` (hover: `#A8A294`) עם מסלול שקוף.
     - הוגדר `overflow-y: scroll !important` קבוע שמונע מראש כל קפיצת רוחב כשהתוכן מתחלף.
  4. **מדידת יציבות מוחלטת (Zero Shift)**:
     - בדיקת Playwright אימתה סטייה של **0.00px ב-X, 0.00px ב-Y ו-0.00px ברוחב** בעת מעבר בין הרשמה להתחברות.
  5. **החזרת אנימציית Slide אופקית (ביטול Bounce)**:
     - בוטלה תזוזת `translateY` שיצרה אפקט של קפיצה מעלה ומטה (bounce).
     - הוחזרה אנימציית החלקה אופקית (`translateX`) קלאסית: בעת מעבר לטאב השמאלי הטופס הנוכחי מחליק ימינה ויוצא, והטופס החדש מחליק משמאל למרכז (ולהיפך), במקביל להחלקת סליידר הטאב.
  6. **הפקת צילומי מסך וסרטון וידאו מאומתים**:
     - צילום מסך של כרטיס ההרשמה הנקי (`auth_signup_clean.png`).
     - צילום מסך של כרטיס ההתחברות הנקי (`auth_login_clean.png`).
     - וידאו מעבר חלק ומאומת (`auth_smooth_perfect.webm`).

### 📅 09/09/2026 (סבב 6) — מרכוז לוגו וכפתור חזרה, הסרת README, ביטול קפיצת ממשק (0.0px) והפקת סרטון
* **פעולות שבוצעו**:
  1. **עיצוב ה-Header מחדש**:
     - לוגו GetAJob מוקם במרכז המדויק באמצעות CSS Grid תלת-עמודתי (`1fr auto 1fr`).
     - כפתור "חזרה" (טקסט מעודכן עם חץ `→ חזרה`) מוקם בצד הימני (RTL).
  2. **הסרת README מטופס ההרשמה**: טקסט המשנה עודכן ל-"כדי לשמור את הניתוחים, תוכנית הפעולה והפרופיל האישי שלכם."
  3. **מניעה מוחלטת של קפיצת ממשק אופקית (Layout Shift: 0.0px)**:
     - הוספת קיבוע פס גלילה (`scrollbar-gutter: stable both-edges !important; overflow-y: scroll !important;`) על כלל עוטפי הדף.
     - שמר על יציבות של 100% — מדידת הדפדפן אימתה סטייה של **0.0 פיקסלים** בדיוק במעבר בין הטאבים!
  4. **הפקת סרטון ואנימציה מלאה**:
     - הקלטת וידאו WebM רציף של מעבר הטאבים.
     - יצירת קובצי אנימציה WebP ו-GIF באיכות גבוהה ושיבוצם ב-walkthrough.md.

### 📅 09/09/2026 (סבב 5) — אנימציית מעבר חלקה ללא רענון דף, עדכון שדות והסרת טולטיפ
* **פעולות שבוצעו**:
  1. **העלמת הודעת "Press enter to apply"**: הוספת כלל CSS חזק על `[data-testid="InputInstructions"]` שמעלים לחלוטין את הטקסט הקופץ בעת לחיצה או כתיבה בכל תיבת קלט. נבדק ואומת בדפדפן.
  2. **הוספת שם משפחה לשדה השם**: עודכנה התווית ל-"שם פרטי ושם משפחה *" עם placeholder "ישראל ישראלי".
  3. **עדכון סקשן "למה בכלל חשבון"**: שורת הפיצ'ר השנייה עודכנה מ-"הורדה חוזרת של README" ל-"שמירת פרטי משתמש ופרופיל" עם ההסבר: "הנתונים, הקבצים והניתוחים שלכם נשמרים בפרופיל האישי ולא נעלמים בסגירת הדפדפן."
  4. **מעבר חלק ואנימטיבי בין הרשמה להתחברות (0ms ללא רענון דף)**:
     - שימוש במבנה כרטיס ראשי יחיד עם סליידר טאבים עליון.
     - שילוב אנימציית החלקה רכה (Sliding Pill) עם `transition: transform 0.28s cubic-bezier(0.4, 0, 0.2, 1)`.
     - מעבר cross-fade רך בין תוכן טופס ההרשמה לטופס ההתחברות (`opacity` ו-`transform: translateY`).
     - חיבור כפתורי הטאב העליונים וקישורי התחתית ("התחברו" / "הירשמו עכשיו") למעבר מיידי ללא רענון עמוד, עם עדכון URL ברקע דרך `history.replaceState`.
  5. **אימות אוטונומי מלא ב-Playwright**: הרצת סקריפט בדיקה מקיף שאימת לחיצות, מעבר טאבים דו-כיווני, תקינות מיקום הפיל והיעלמות הטולטיפ, ושמירת צילומי מסך מאומתים.

### 📅 09/09/2026 (סבב 4) — שכתוב מדויק ומושלם של מסך ההרשמה וההתחברות לפי עיצוב היעד
* **פעולות שבוצעו**:
  1. **פיצול כרטיס שמאל (הפיצ'רים)**: הופרד לשני כרטיסים נפרדים בדיוק לפי העיצוב:
     * כרטיס עליון: 3 פיצ'רים (היסטוריית ניתוחים, הורדה חוזרת של README, מעקב לאורך זמן) עם אייקונים בריבוע סגול בהיר מימין לטקסט (RTL מלא).
     * כרטיס תחתון נפרד: באנר קטן עם נקודה ירוקה (Badge) מימין וטקסט 'הניתוח הראשון פתוח בלי חשבון...'.
  2. **הסרת הקופסה/אינפוט הריק**: ביטול מוחלט של אלמנטים ריקים מעל הטאבים באמצעות מעבר למבנה נקי ב-`st.form` ומניעת unclosed HTML tags.
  3. **טאב מצבים מאוחד**: קונטיינר אפור בהיר עוטף אחד שבו הטאב הפעיל מקבל רקע לבן מוגבה עם פינות מעוגלות וצל.
  4. **שדות קלט ומד סיסמה**: גבולות מלאים ועגולים עם צל פנימי (`data-baseweb="base-input"`), שמירה על אייקון עין נקי בסיסמה, ומד חוזק 3 מקטעים (סגול/אפור) עם כיתוב הנחיה.
  5. **תיבת סימון (Checkbox)**: כרטיסון חמים מעוגל עם יישור תקני של התיבה מימין והטקסט משמאל, תוך הדגשת קישורי תנאי השימוש ומדיניות הפרטיות בסגול אינדיגו ללא שבירת שורות.
  6. **כפתור הגשה ועימוד**: כפתור סגול בולט ומעוגל עם צל עמוק, מרכוז מלא של המסך ברוחב 940px בלבד.
  7. **בדיקה עצמאית ב-Playwright**: נבדקו וצולמו שני המצבים (הרשמה והתחברות) ואומתו כתואמים 1:1 למפרט.

### 📅 09/09/2026 (סבב 4) — יישור ואיזון גובה כרטיסיות תפקיד יעד (Step 2)
* **פעולות שבוצעו**:
  1. זיהוי פער גובה בין כרטיסיית "מעצב/ת UX/UI" לשאר הכרטיסיות (עקב שבירת שורה שלישית בתיאור ואי-מתיחה אחידה של ה-columns).
  2. הידוק ניסוח תיאור התפקיד של UX/UI כך שיתפרס על 2 שורות בדיוק כמו שאר 3 הכרטיסיות.
  3. הגדרת `height: 42px`, `min-height: 42px` ו-`-webkit-line-clamp: 2` על מחלקת `.step2-role-desc`.
  4. הגדרת `align-self: stretch` ומתיחה לגובה 100% על כלל עמודי הכרטיסיות.
  5. הזרקת הסגנונות דרך `render_clean_html` למניעת פרשנות כקוד ב-Markdown.
  6. אימות ויזואלי ומדידת פיקסלים מלאה בדפדפן (כל 4 הכרטיסיות בגובה אחיד של 170px בדיוק).

### 📅 09/09/2026 (סבב 3) — הגדרת מנוע דפדפן אוטונומי ובדיקת ממשק ויזואלית
* **פעולות שבוצעו**:
  1. התקנת Playwright ובינארי דפדפן Chromium ייעודי בסביבה המקומית.
  2. בניית תשתית לבדיקה אוטונומית וצילומי מסך עצמאיים ישירות מול `http://localhost:8501`.
  3. גילוי ותיקון תצוגת אייקון סיסמה (Material Symbols `visibility`) שנפגעה בשל דריסת פונט גלובלית ב-CSS — כעת מוצגת עין נקייה לחלוטין.
  4. ביצוע צילום מסך מלא ואימות ויזואלי של טופס ההרשמה והפאנל הצדדי — תואם 100% למפרט העיצוב Claude v1.0.

### 📅 09/09/2026 (סבב 2) — הקמת מסך הרשמה, התחברות ואזור אישי
* **פעולות שבוצעו**:
  1. יצירת [auth_view.py](file:///C:/Users/osher.a/.gemini/antigravity-ide/scratch/GetAJob/auth_view.py) המממש במדויק את מפרט העיצוב של Claude Artifact v1.0.
  2. שימור ה-Header הקיים של GetAJob ללא שינוי, תוך חיבור קישורי "הירשם" ו"היכנס" למעבר מיידי למסך ההרשמה וההתחברות (`?step=auth&mode=signup` / `mode=login`).
  3. הטמעת שדה "שם ושם משפחה" והגדרת ולידציה קשיחה שכל השדות הינם חובה.
  4. עדכון פאנל הערך הצדדי לניסוח "שמירת נתונים ומעקב התקדמות".
  5. הטמעת מערכת ניהול משתמשים מקומית (`data/users.json`), התחברות, איפוס סיסמה ואזור אישי עם היסטוריית ניתוחים.
  6. אתחול שרת ה-Dev המקומי עם `--server.runOnSave true` לטעינה חמה של כל העדכונים.
  7. תיקון מחרוזת תבנית ה-Navbar ב-[ui_styles.py](file:///C:/Users/osher.a/.gemini/antigravity-ide/scratch/GetAJob/ui_styles.py) ואימות קומפילציה מלא (`py_compile`) ללא שגיאות.
* **סטטוס בדיקה**: השרת עבר קומפילציה תקינה, עודכן ורץ מקומית ב-[http://localhost:8501](http://localhost:8501).

### 📅 09/09/2026 (סבב 1) — אתחול סביבה והקמת מעקב התקדמות
* שיבוט המאגר מ-GitHub והקמת סביבה וירטואלית ייעודית (`.venv`).
* התקנת כל התלויות מ-[requirements.txt](file:///C:/Users/osher.a/.gemini/antigravity-ide/scratch/GetAJob/requirements.txt).
* הפעלת שרת Streamlit בתצורת Daemon בפורט 8501 ויצירת [PROGRESS.md](file:///C:/Users/osher.a/.gemini/antigravity-ide/scratch/GetAJob/PROGRESS.md).

---

## 🎯 4. יעדים ומשימות פתוחות (Next Steps / Backlog)
* [x] ~~**מסך הרשמה והתחברות** (הושלם בדיוק לפי מפרט Claude)~~
* [ ] **מסך 2 — בחירת תפקיד והזנת קו"ח**: ליטוש חוויית המשתמש והעלאת קבצים בהתאם למפרט v1.0.
* [ ] **מסך 3 — שדרוג דף תוצאות ואבחון**: בדיקת התאמת כרטיסיות ה-ATS, תרשים הפערים והמסלולים לשפת העיצוב.
* [ ] **מנגנון שמירה / ייצוא**: ייצוא תוכנית העבודה או קורות החיים המשודרגים (PDF/טקסט).

---

## 📌 5. כללי עבודה מקוצרים (Quick Reference)
* **עברית ויישור**: כל פלט למשתמש בעברית מיושר לימין (`<div dir="rtl" style="text-align: right;">`).
* **קוד וטרמינל**: באנגלית בלבד.
* **עריכות קוד**: שימוש בעריכות ממוקדות שורות (`replace_file_content`) בלבד — אין צורך לטעון או להדפיס קבצים שלמים.
* **עבודה מקומית ודחיפה לענן**: עובדים באופן שוטף אך ורק מול השרת המקומי; דחיפה ל-GitHub/ענן תבוצע **אך ורק** בסיום יום העבודה בהוראה מפורשת ("מסיימים עבודה להיום").

