# 🚀 GetAJob - תמונת מצב והנחיות להמשך עבודה (Project State & Continuity)

מסמך זה מרכז את כל המידע הנדרש להמשך עבודה רציף ומדויק על הפרויקט מכל מחשב (לרבות מחשב העבודה).

---

## 📌 1. סקירת הפרויקט ומטרתו
**GetAJob** היא פלטפורמת AI ישראלית לקריירה והכנה לעבודה בהייטק, המיועדת לפרויקט גמר לימודים.  
המערכת מנתחת קורות חיים של מועמדים מול דרישות השוק במגוון מקצועות (עיצוב UX/UI, ניתוח נתונים, פיתוח תוכנה, DevOps, QA, סייבר ועוד).

---

## 🎨 2. עיצוב ושפה גרפית — GetAJob Design Handoff v1.0 (מחייב בלבד)
הממשק הוסב ומבוסס באופן מלא ובלעדי על **מפרט מערכת עיצוב GetAJob v1.0** (Claude Design Handoff, 8 בספטמבר 2026).
**כלל ברזל לפרויקט:** לפני תחילת עבודה על כל מסך חדש (בחירת מסלול, ניתוח קו"ח, הרשמה/התחברות), חובה לוודא מול המשתמש יישור קו מלא להנחיות מסמך זה!

1. **פלטה ורקעים (סעיף 1):**
   - מצב Light בלבד (הכיוון הכהה בוטל).
   - רקע בסיס: `#FBF9F4` עם שכבת חתימה ויזואלית של שני גרדיאנטים רדיאליים ב-`background-attachment: fixed`.
   - משטחים: כרטיסים ופאנלים ב-`--surface` (`linear-gradient(180deg, #FFFFFF, #FDFCF9)`), מסגרת `#EEE8DA`, וכרטיסים פנימיים/שקועים ב-`--surface-sunken` (`linear-gradient(180deg, #FCFAF6, #F5F1E7)`).
   - אקסנט: אינדיגו `#4F46E5`, קצה עליון לכפתור `#6C63FF`, מסגרת `#4338CA`.
   - טקסט: כותרות ומספרים `#17171C`, גוף טקסט `#4A4A55`, מטא `#6B6B74` (אין להבהיר מתחת לכך).
2. **טיפוגרפיה (סעיף 2):** גופן יחיד בלבד בממשק — **Heebo** (משקלים 400–800). כל הכותרות עם `text-wrap: pretty`. JetBrains Mono שמור אך ורק לקוד.
3. **מרווחים, גריד ורדיוסים (סעיף 3):** סקאלת בסיס 4px, מכולה מרכזית `max-width: 1180px; padding-inline: 32px;`, מרווח בין סקשנים 96px, רדיוס כפתורים ושדות 12px, כרטיסים 18px, פאנלים 22px, פסי CTA ב-26px.
4. **מפרט רכיבים (סעיף 5):**
   - **כפתורים:** Primary אחד בלבד בכל מסך (`linear-gradient(180deg, #6C63FF, #4F46E5)` עם מסגרת `#4338CA` וצל `--sh-accent`). כפתור משני לבן עם מסגרת `#EEE8DA`. כפתור Dark `#111114` ברדיוס 11px.
   - **Hardware Badges:** 3 דרגות סמנטיות בלבד — `חוסם` (`#B42318` על `#FEE2E2`), `חשוב` (`#92400E` על `#FEF3C7`), `קיים` (`#166534` על `#DCFCE7`).
   - **Match Meter:** מד התאמה אופקי יחיד (לא טבעת ולא מקטעים), מילוי אינדיגו בלבד בכל ציון.
   - **ניווט צף (סעיף 5.6):** `surface-glass` מטושטש (blur 14px), מרכוז מלא ע"י CSS Grid `1fr auto 1fr` (ללא `space-between`), וכפתור חזרה לראש העמוד בפינה שמופיע מעל 320px גלילה.
   - **פס CTA כהה (סעיף 5.7):** רקע `#111114` יוקרתי עם גרדיאנט פנימי.
5. **כללי איסור קשיחים (סעיף 7):**
   - ללא אימוג'י כלל בממשק (אייקוני SVG נקיים בלבד).
   - ללא צבעים סמנטיים (ירוק/כתום/אדום) על כפתורים או מסגרות של כרטיסים שלמים.
   - ללא רוחב קבוע, גובה קבוע או `white-space: nowrap` על טקסט בעברית.
   - ללא שני כפתורי Primary באותו מסך.

---

## 🛠️ 3. פיצ'רים מרכזיים שהוטמעו
1. **דף נחיתה ו-Hero משודרג (לפי Claude Design Handoff v1.0):**
   - **Navbar נקי:** הוסר אינדיקטור "המערכת פעילה", שומרו כפתורי "הירשם" ו"היכנס" עם אפקט זכוכית מטושטשת.
   - **כותרת ותיאור Hero ממורכזים:** שתי שורות הכותרת ופסקת התיאור תחתיה ממורכזות במדויק (Center Aligned) זו מול זו וביחס למסך.
   - **4 כרטיסיות ערך ב-Hero:** גריד רספונסיבי מותאם (4 טורים בדסקטופ, 2 בטאבלט, 1 במובייל) המציג את 4 יתרונות המערכת.
   - **סקשן 'איך זה עובד' מהודק:** הוסר תת-המשפט המיותר, איור המערכת מוטמע בעמודת שמאל והשלבים מימין.
   - **דוגמת פלט מהודקת ומיושרת:** כותרות ממורכזות עם ריווח מהודק, מד התאמה ללא שטח מת עם תצוגת `64%` (סימן האחוז מימין למספר), וסרגל התקדמות מותאם לימין.
   - **שאלות נפוצות (FAQ) ב-2x2:** 4 כרטיסיות מעוצבות (2 שורות של 2), כולל שאלה חדשה על מעבר סינון ATS.
   - **באנר כהה תחתון:** כותרת קריאה בלבן בהיר, הוסרו כפתורי רישום/התחברות מיותרים, ונשמר כפתור יחיד ממורכז: "הריצו ניתוח להדגמה".
   - **ריווח אחיד ונושם:** מרווח של 80px מדויק בין כל הסקשנים בעמוד.
2. **צ'קליסט אבני דרך אינטראקטיבי (Career Progress Tracker):**
   - פותח במיוחד עבור פרויקט הגמר בהתאמה לכל תפקיד (UX/UI, Data, Dev וכו').
   - מזהה את החוסרים הספציפיים של המועמד ומציג משימות ממוקדות לסגירת הפער.
   - כולל מעקב התקדמות עם מד התקדמות ליניארי של Material Design 3.
   - מנגנון נגישות מלא (`label_visibility="collapsed"`).
3. **דף בחירת תפקיד והעלאת קו"ח:**
   - רכיבי M3 Filter Chips (`st.pills`) לבחירה מהירה בין תפקידים מובילים (UX/UI, Frontend, Data).
   - תרחישי דמו מהירים וטעינת קבצי PDF.
4. **דף תוצאות ואבחון:**
   - ציון התאמה דינמי, ניתוח פערי מיומנויות (High / Medium / Low).
   - שדרוג קו"ח לפורמט ATS (לפני / אחרי).
   - סילבוס למידה מותאם אישית.

---

## ⚙️ 4. קונפיגורציה וטעינה חמה (Hot Reload)
- הקובץ [.streamlit/config.toml](file:///c:/Users/Osher/Desktop/GetAJob/.streamlit/config.toml) הוגדר עם:
  ```toml
  [server]
  headless = true
  runOnSave = true
  ```
  הגדרה זו מבטיחה שכל שמירת קובץ תתרענן מיידית בשרת ללא צורך בהפעלה מחדש.

---

## 💻 5. צעדים להמשך עבודה ממחשב העבודה
1. **משיכת הקוד העדכני:**
   ```bash
   git pull origin main
   ```
   *(אם הפרויקט טרם שובט: `git clone https://github.com/osher66/GetAJob.git`)*
2. **התקנת תלויות (במידת הצורך):**
   ```bash
   pip install -r requirements.txt
   ```
3. **הרצת השרת המקומי בעבודה:**
   ```bash
   streamlit run app.py
   ```
   הממשק ייפתח בכתובת: `http://localhost:8501`.
   *(הערה: המערכת כוללת מנוע ניתוח דינמי מובנה ותרחישי דמו מלאים, כך שאין צורך בהגדרת מפתח API כדי להריץ ולהציג את כל הפיצ'רים).*
5. **המשך שיחה עם סוכן ה-AI (Antigravity):**
   בפתיחת השיחה במחשב העבודה, כל מה שצריך לכתוב לסוכן הוא:
   > "אני ממשיך את העבודה על GetAJob. קרא את PROJECT_STATE.md ואת .agents/rules/workflow.md כדי להכיר את המצב הקיים, ובוא נמשיך מאיפה שעצרנו."

---

## 📐 6. כללי תקשורת ועבודה מול סוכן ה-AI (Workflow Rules)
1. **יישור לימין (RTL):** כל תשובה וסיכום סטטוס למשתמש נכתבים בעברית ביישור מוחלט לימין.
2. **טקסט משולב עברית-אנגלית (Bi-directional):** מונחים טכניים באנגלית, שמות קבצים, מספרים וסימני פיסוק משולבים בצורה נקייה וקריאה ללא היפוך סדר משפטים או שבירת שורות.
3. **חיסכון בטוקנים ותמציתיות:** תשובות ממוקדות בבולטים קצרים בלבד, ללא מילות נימוס מיותרות, התנצלויות או הקדמות.
4. **קוד והנמקה טכנית:** כל ה-Reasoning, פקודות הטרמינל, הודעות ה-Git, והקוד עצמו נשארים באנגלית בלבד.

